---
phase: 02-dashboard-cl1-theater
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - public/js/terminal.js
  - public/css/terminal.css
  - public/js/app.js
autonomous: true

must_haves:
  truths:
    - "Terminal scrolls neural decision logs narrating every trade action"
    - "Buy events show coherence spike -> pattern match -> confidence score -> EXECUTING BUY narrative"
    - "Sell events show exit reasoning in neural language"
    - "Reject events show rug detection in neural language"
    - "Ambient neural chatter fills idle periods (never silent)"
    - "Toggle button switches between raw and structured terminal views"
    - "Auto-scroll pinned to bottom, jump-to-latest button appears on scroll up"
    - "Terminal caps at 500 lines to prevent memory leak"
    - "Dashboard looks identical in SAFE and LIVE mode"
  artifacts:
    - path: "public/js/terminal.js"
      provides: "Terminal log manager with idle chatter and dual view"
      contains: "IDLE_MESSAGES"
    - path: "public/css/terminal.css"
      provides: "Terminal panel styles with color coding"
      contains: "term-line"
  key_links:
    - from: "public/js/app.js"
      to: "public/js/terminal.js"
      via: "Terminal.handleMessage(msg)"
      pattern: "Terminal"
    - from: "public/js/terminal.js"
      to: "idle chatter timer"
      via: "resetIdleTimer on real events"
      pattern: "lastEventTime"
---

<objective>
Build the terminal console with neural decision narrative, idle chatter, dual-view toggle, and wire the complete message router in app.js to connect all panels. This completes the dashboard's narrative layer.

Purpose: The terminal is what makes this look like a biological neural network making decisions. Every trade action gets narrated in clinical-scientific neural language. Idle chatter keeps it feeling alive.
Output: Fully functional terminal with trade narratives, idle chatter, and complete WS message routing to all panels.
</objective>

<execution_context>
@C:\Users\thedi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\thedi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-dashboard-cl1-theater/02-RESEARCH.md
@.planning/phases/02-dashboard-cl1-theater/02-CONTEXT.md
@.planning/phases/02-dashboard-cl1-theater/02-01-SUMMARY.md
@src/core/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Terminal console with neural narratives and idle chatter</name>
  <files>
    public/js/terminal.js
    public/css/terminal.css
  </files>
  <action>
    1. Create `public/js/terminal.js`:
       - Expose `window.Terminal` object with `init()`, `handleMessage(msg)`, `appendLine(text, className)` methods
       - Constants: MAX_LINES = 500, IDLE_TIMEOUT = 8000 (8s before chatter starts), IDLE_INTERVAL = 3000 (3s between idle messages)
       - State: lastEventTime, userScrolled flag, idleTimer, currentView ('raw'|'structured')
       - IDLE_MESSAGES array (8-10 messages): "monitoring coherence baseline...", "scanning mempool activity...", "electrode drift: nominal", "culture health: stable", "entropy within bounds", "synaptic noise floor: low", "awaiting signal cascade...", "baseline oscillation: theta range", "membrane potential: resting", "axon conductance: normal"

       - `appendLine(text, className, structured)`:
         - Create div.term-line with className
         - In raw view: prepend timestamp [HH:MM:SS.mmm]
         - In structured view: format with visual separators and grouping
         - Append to #terminal container
         - Trim to MAX_LINES (remove oldest)
         - Auto-scroll to bottom if !userScrolled
         - Reset idle timer (call resetIdleTimer())

       - `handleMessage(msg)`: Convert WS messages to neural narrative sequences:

         signal:detected (BUY):
           - "coherence spike detected on electrode cluster E-{random 1-64}"
           - "pattern recognition: token {mint_short} -- cross-referencing signal matrix..."

         signal:validated:
           - "substrate integrity check: PASSED"
           - "neural confidence: {0.7-0.95 random} -- threshold exceeded"
           - ">> SIGNAL LOCKED: {mint_short}"
           - Add visual separator line (div.term-separator)

         signal:rejected:
           - "substrate integrity check: FAILED"
           - "anomaly detected: {reason} -- neural pathway inhibited"
           - ">> SIGNAL REJECTED: {mint_short}"
           - Add separator

         trade:executed:
           - Separator line (div.term-separator with "thinking" dots animation)
           - "executing synaptic relay... routing {solAmount} SOL"
           - ">> TRADE EXECUTED: {mint_short} @ {entryPrice}"
           - "position acquired -- monitoring decay curve"

         position:opened:
           - "new neural pathway established: {mint_short}"
           - "exit parameters: TP {takeProfitPct}% | SL {stopLossPct}% | TTL {timeLimitMs/60000}min"

         position:closed:
           - Separator
           - If reason is stop-loss: "WARNING: membrane potential collapse on {mint_short}"
           - If reason is take-profit: "optimal exit point reached -- harvesting synaptic gains"
           - If reason is time-limit: "temporal decay threshold exceeded -- pruning pathway"
           - If reason is copy-sell: "external stimulus detected -- mirroring neural response"
           - ">> POSITION CLOSED: {mint_short} | PNL: {pnlPct}% | reason: {reason}"
           - Color: green class for positive PNL, red for negative

         price:updated: no terminal output (too noisy)

         snapshot: for each position, log "restoring neural pathway: {mint_short}"

       - Idle chatter system:
         - resetIdleTimer(): clear existing timer, set new setTimeout(startIdleChatter, IDLE_TIMEOUT)
         - startIdleChatter(): pick random IDLE_MESSAGE, appendLine with 'idle' class (dimmer color), set next idle message via setTimeout(startIdleChatter, IDLE_INTERVAL + random(0-2000ms))
         - On any real event: resetIdleTimer() cancels idle sequence
         - lastEventTime tracks to avoid idle during active periods

       - Scroll detection:
         - Listen to #terminal scroll event
         - If scrollTop + clientHeight < scrollHeight - 50: set userScrolled = true, show #jump-to-latest button
         - If scrollTop + clientHeight >= scrollHeight - 10: set userScrolled = false, hide button
         - #jump-to-latest click: scrollTop = scrollHeight, userScrolled = false, hide button

       - View toggle:
         - Toggle button in terminal panel header switches currentView between 'raw' and 'structured'
         - Raw view: plain timestamped lines, monospace, no grouping
         - Structured view: grouped by event with visual cards/boxes, separator lines between events, key data highlighted
         - Toggle re-renders visible lines in new format (or simpler: just toggle CSS classes that show/hide formatting elements)

    2. Create `public/css/terminal.css`:
       - #terminal: height 100%, overflow-y auto, padding 4px 8px, font-family var(--font-mono), font-size 0.85rem, line-height 1.5
       - .term-line: padding 1px 0, white-space pre-wrap, word-break break-all
       - .term-line .timestamp: color var(--text-dim), margin-right 8px
       - Color classes:
         - .term-line.signal: color var(--accent-teal)
         - .term-line.trade: color var(--accent-blue)
         - .term-line.reject: color var(--accent-pink)
         - .term-line.close-win: color var(--accent-green)
         - .term-line.close-loss: color var(--accent-red)
         - .term-line.idle: color var(--text-dim), font-style italic
         - .term-line.system: color var(--accent-olive)
         - .term-line.warning: color var(--accent-red)
       - .term-separator: height 1px, background linear-gradient(to right, transparent, var(--border), transparent), margin 4px 0
       - .term-separator.thinking: background repeating pattern with animated dots (CSS animation)
       - #jump-to-latest: position absolute (relative to panel), bottom 8px, right 8px, background var(--accent-teal), color var(--bg-primary), border none, padding 4px 12px, border-radius 4px, cursor pointer, font-family var(--font-mono), font-size 0.75rem, display none (shown via JS)
       - .view-toggle: small button in panel header, background transparent, border 1px solid var(--border), color var(--text-secondary), cursor pointer, font-size 0.7rem, padding 2px 8px
       - Structured view styles: .structured .term-line grouped in .event-group with left border accent color, padding-left

       - Thin dark scrollbar:
         - #terminal::-webkit-scrollbar: width 6px
         - #terminal::-webkit-scrollbar-track: background var(--bg-primary)
         - #terminal::-webkit-scrollbar-thumb: background var(--border), border-radius 3px
  </action>
  <verify>
    - Start server, open browser
    - Terminal shows boot-related log after boot sequence
    - After ~8s of no events, idle chatter messages appear in dim italic
    - If pipeline emits events, terminal shows neural narrative (color-coded)
    - Toggle button switches between raw and structured views
    - Scroll up: "jump to latest" button appears. Click it: scrolls to bottom
    - Terminal never exceeds 500 lines in DOM
  </verify>
  <done>
    Terminal console shows neural decision narratives for all trade events, idle chatter during quiet periods, dual-view toggle, auto-scroll with jump-to-latest, and 500-line cap.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire complete message router and finalize app.js</name>
  <files>
    public/js/app.js
    public/index.html
  </files>
  <action>
    1. Update `public/js/app.js` message router (replace console.log placeholders from plan 01):
       - routeMessage(msg) switch cases:
         - 'snapshot': Positions.handleMessage(msg), Terminal.handleMessage(msg), NeuronGrid.handleMessage(msg)
         - 'signal:detected', 'signal:validated', 'signal:rejected': Terminal.handleMessage(msg), NeuronGrid.handleMessage(msg), BioStats.handleMessage(msg)
         - 'trade:executed': Terminal.handleMessage(msg), NeuronGrid.handleMessage(msg), BioStats.handleMessage(msg)
         - 'position:opened', 'position:updated': Positions.handleMessage(msg), BioStats.handleMessage(msg)
         - 'position:closed': Positions.handleMessage(msg), Terminal.handleMessage(msg), NeuronGrid.handleMessage(msg), BioStats.handleMessage(msg)
         - 'price:updated': Positions.handleMessage(msg), BioStats.handleMessage(msg), NeuronGrid.handleMessage(msg), Stats.update()

       - DOMContentLoaded init sequence:
         - NeuronGrid.init()
         - Stats.init()
         - Positions.init() (if it has init)
         - Terminal.init()
         - Boot.onComplete callback: set bootComplete, show dashboard, drainQueue, start idle chatter timer
         - Boot.play()
         - connectWebSocket()

    2. Update `public/index.html`:
       - Ensure ALL script tags are present in correct order:
         bio-stats.js, confidence.js, stats.js, neuron-grid.js, positions.js, terminal.js, boot.js, app.js
       - Add link to css/terminal.css and css/neuron-grid.css in head
       - Add #jump-to-latest button inside terminal panel (positioned absolute)
       - Add .view-toggle button in terminal panel header
       - Verify all element IDs match what JS files expect:
         neuron-canvas, positions-list, terminal, stat-neurons, stat-coherence, stat-entropy, stat-firing-rate, stat-win-rate, jump-to-latest

    3. Ensure MODE-03 compliance (SAFE/LIVE indistinguishable):
       - No reference to "SAFE", "paper", "simulation" anywhere in frontend code
       - No mode indicator in status bar or terminal
       - All trade events look real regardless of mode
  </action>
  <verify>
    - Start server with pipeline running (`npx tsx src/server/index.ts`)
    - Open http://localhost:{port} -- full dashboard loads
    - Boot sequence plays, then dashboard appears
    - All three panels functional: neuron grid animating, positions showing (if any), terminal scrolling
    - Stats bar updating
    - Idle chatter appears during quiet periods
    - No "SAFE" or "paper trade" text visible anywhere
    - No JS errors in console
    - `npx tsc --noEmit` still passes (server-side)
  </verify>
  <done>
    Complete dashboard wired end-to-end. All WebSocket messages route to correct panels. Boot -> grid -> positions -> terminal -> stats all functional. No SAFE/LIVE visual distinction.
  </done>
</task>

</tasks>

<verification>
- Terminal narrates trade actions in neural language
- Idle chatter fills silence after 8 seconds
- Toggle switches between raw and structured views
- Auto-scroll works, jump-to-latest appears on scroll up
- All panels receive and react to WebSocket messages
- No SAFE/LIVE mode distinction visible
- No memory leaks (terminal capped at 500 lines)
- Full end-to-end flow: pipeline event -> bus -> WebSocket -> browser -> correct panel
</verification>

<success_criteria>
- Terminal shows coherence/pattern/confidence narrative for buys
- Terminal shows exit reasoning in neural language for sells
- Idle chatter never stops during quiet periods
- View toggle works between raw and structured
- Complete message routing: every WS message type handled
- MODE-03: zero visual difference between SAFE and LIVE
- All 11 requirements covered: DASH-01 through DASH-06, CL1-01 through CL1-04, MODE-03
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-cl1-theater/02-03-SUMMARY.md`
</output>
